def count_even_digits(n):
    count = 0
    for digit in str(n): 
        if int(digit) % 2 == 0:
            count += 1 
    if(count >= 2):
        return True
    else:
        return False
n =123579
print(count_even_digits(n))
